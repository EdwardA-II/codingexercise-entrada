<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="en">
<head>
    <title>Submission</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>Submission Successful</h1>

    <div th:if="${newEntry != null}" class="new-entry">
        <h2>Details of Your Submission</h2>
        <p><strong>First Name:</strong> <span th:text="${newEntry.firstName}"></span></p>
        <p><strong>Last Name:</strong> <span th:text="${newEntry.lastName}"></span></p>
        <p><strong>Age:</strong> <span th:text="${newEntry.age}"></span></p>
        <p><strong>Title:</strong> <span th:text="${newEntry.title}"></span></p>
        <p><strong>Hometown:</strong> <span th:text="${newEntry.hometown}"></span></p>
    </div>

    <h2>All Previous Entries</h2>
    <table>
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Age</th>
                <th>Title</th>
                <th>Hometown</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="entry : ${entries}">
                <td th:text="${entry.firstName}"></td>
                <td th:text="${entry.lastName}"></td>
                <td th:text="${entry.age}"></td>
                <td th:text="${entry.title}"></td>
                <td th:text="${entry.hometown}"></td>
            </tr>
        </tbody>
    </table>

    <a href="/entries">Submit Another Entry</a>
</body>
</html>